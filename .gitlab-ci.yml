stages:
  - install
  - build

install-job:
  stage: install
  tags:
    - npm
  script:
    - npm ci


build-job:
  stage: build
  tags:
    - npm
  script:
    - npm install -g @angular/cli 
    - ng build
